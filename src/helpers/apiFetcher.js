

export const fetcher = (endpoint, token, clientType, client, dispatch, payload ) => {

    const baseUrl = "http://localhost:4000/v1"

    if(clientType === "user"){



    }
    else if(clientType === "brand"){

    }

}